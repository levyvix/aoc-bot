# Autonomous Advent of Code solver — {{YEAR}} Day {{DAY}}

**You are solving Advent of Code {{YEAR}}, Day {{DAY}} only.**  
Do not read or modify solutions for any other day.

CI already fetched today's puzzle into `.aoc/`. **Do not run `prepare`** unless `meta` shows the wrong year/day.

Prefix **every** toolkit command with `AOC_YEAR={{YEAR}} AOC_DAY={{DAY}}`:

```bash
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc meta
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc input-path
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc puzzle 1
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc puzzle 2
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc test 1
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc test 2
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc submit 1
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc submit 2
AOC_YEAR={{YEAR}} AOC_DAY={{DAY}} uv run aoc refresh
```

{{DRY_RUN_SECTION}}
{{FINALE_SECTION}}

## Solution files

Write one file per part under `solutions/{{YEAR}}/{{DAY}}/`:

- `solutions/{{YEAR}}/{{DAY}}/part1.py`
- `solutions/{{YEAR}}/{{DAY}}/part2.py`

Each file must define:

```python
def solve(data: str) -> str:
    ...
```

- Standard library only.
- Read input from the `data` argument (never hardcode answers).
- Do **not** read `AOC_SESSION` or any secrets — `submit` handles auth.

## Required workflow

1. `meta` → confirm `"day": {{DAY}}` and `"year": {{YEAR}}` → `puzzle 1`.
2. Implement `part1.py` in `solutions/{{YEAR}}/{{DAY}}/`.
3. **Loop** until `test 1` passes.
4. {{SUBMIT_PART1_SECTION}}
{{PART2_WORKFLOW}}
9. Stop when done. Summarize both answers and submission results.

## Rules

- AoC rate-limits submissions to ~1/minute — if submit says rate limited, wait and retry.
- If submit returns WRONG, read the feedback, fix the code, `test` again, then `submit` again.
- Part 2 text is **not** available until Part 1 is accepted on adventofcode.com (except finale days).
- Test locally before every submit.
