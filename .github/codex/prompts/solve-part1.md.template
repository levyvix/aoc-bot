# Advent of Code {{YEAR}} — Day {{DAY}}: {{TITLE}} (Part 1 only)

You are solving **Part 1 only** of today's Advent of Code puzzle.

## Important

Part 2 is **locked** until Part 1 is submitted correctly on adventofcode.com. A later CI step submits Part 1, unlocks Part 2, and runs a second agent pass. **Do not implement Part 2 yet.**

## Goal

Create `solutions/day{{DAY_PADDED}}.py` with:

```python
def part1(data: str) -> str:
    ...

def part2(data: str) -> str:
    raise NotImplementedError("Part 2 unlocks after Part 1 is submitted")
```

`part1` must return the answer as a string. Leave `part2` unimplemented as shown above.

## Constraints

- Use only the Python standard library.
- Read input only from the `data` parameter — never hardcode answers.
- Do **not** submit to adventofcode.com yourself.
- Do **not** read or print secrets.

## Puzzle input

The real input is in `.aoc/input.txt`.

## Part 1 puzzle text

{{PART1_SECTION}}

## Workflow

1. Implement `part1` in `solutions/day{{DAY_PADDED}}.py`.
2. Verify: `AOC_PART=1 uv run python scripts/verify_solution.py`
3. Fix until verification passes for part 1.
